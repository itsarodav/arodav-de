---
import Icon from './Icon.astro';
import { useTranslations, getAlternatePath, type Lang } from '@/i18n';

interface Props {
  lang: Lang;
  currentPath: string;
}

const { lang, currentPath } = Astro.props;
const t = useTranslations(lang);

const alternateLang = lang === 'en' ? 'es' : 'en';
const alternatePath = getAlternatePath(currentPath, alternateLang as any);

const homePath = lang === 'es' ? '/es' : '/';
const writingPath = lang === 'es' ? '/es/writing' : '/writing';

const navLinks = [
  { label: t('nav.home'), href: homePath },
  { label: t('nav.writing'), href: writingPath },
  { label: t('nav.design'), href: 'https://design.arodav.com/', external: true },
];
---

<div class="mobile-menu" data-mobile-menu aria-hidden="true">
  <div class="mobile-menu__panel">
    <nav class="mobile-menu__nav" aria-label={t('nav.main')}>
      <ul class="mobile-menu__list">
        {navLinks.map((link) => (
          <li>
            <a
              class="mobile-menu__link"
              href={link.href}
              target={link.external ? '_blank' : undefined}
              rel={link.external ? 'noopener noreferrer' : undefined}
              aria-current={currentPath === link.href ? 'page' : undefined}
            >
              {link.label}
              {link.external && <Icon name="arrow-out" size={22} color="currentColor" role="decorative" />}
            </a>
          </li>
        ))}
      </ul>
      <a
        class="mobile-menu__lang-btn"
        href={alternatePath}
        aria-label={t('nav.language_label')}
      >
        {t('nav.language')}
      </a>
    </nav>
  </div>
</div>

<style lang="scss">
  @use '../styles/tokens' as *;

  .mobile-menu {
    position: fixed;
    inset: 0;
    z-index: 200;
    pointer-events: none;

    &[aria-hidden='false'] {
      pointer-events: auto;

      .mobile-menu__panel {
        opacity: 1;
        transform: translateY(0);
      }
    }

    &__panel {
      position: absolute;
      inset: 0;
      background-color: var(--color-bg-primary);
      opacity: 0;
      transform: translateY(-8px);
      transition: opacity 0.3s cubic-bezier(0.4, 0, 0.2, 1),
                  transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      display: flex;
      flex-direction: column;
      align-items: center;
      padding-top: calc($space-8 + 3rem);
      padding-bottom: $space-8;
    }

    &__nav {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      width: 100%;
      max-width: 640px;
      padding: 0 $space-4;
    }

    &__list {
      display: flex;
      flex-direction: column;
      gap: $space-6;
    }

    &__link {
      display: inline-flex;
      align-items: center;
      gap: $space-2;
      font-size: $text-2xl;
      font-weight: $font-medium;
      letter-spacing: $tracking-tighter;
      color: var(--color-text-primary);
      transition: color 0.2s ease;

      &[aria-current='page'] {
        color: var(--color-link);
      }

      @media (hover: hover) {
        &:hover {
          color: var(--color-link);
        }
      }
    }

    &__lang-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      align-self: flex-start;
      padding: $space-2 $space-4;
      border: 1px solid var(--color-border);
      border-radius: 999px;
      font-size: $text-sm;
      font-weight: $font-semibold;
      letter-spacing: $tracking-tight;
      color: var(--color-text-secondary);
      transition: border-color 0.2s ease, color 0.2s ease;

      @media (hover: hover) {
        &:hover {
          border-color: var(--color-text-primary);
          color: var(--color-text-primary);
        }
      }
    }
  }
</style>

<script is:inline>
  document.addEventListener('DOMContentLoaded', function () {
    var menu = document.querySelector('[data-mobile-menu]');
    if (!menu) return;

    var toggleBtns = document.querySelectorAll('[data-menu-toggle]');

    function open() {
      menu.setAttribute('aria-hidden', 'false');
      document.body.style.overflow = 'hidden';
      toggleBtns.forEach(function (btn) { btn.setAttribute('aria-expanded', 'true'); });
      var firstLink = menu.querySelector('a');
      if (firstLink) firstLink.focus();
    }

    function close() {
      menu.setAttribute('aria-hidden', 'true');
      document.body.style.overflow = '';
      toggleBtns.forEach(function (btn) { btn.setAttribute('aria-expanded', 'false'); });
      var trigger = document.querySelector('[data-menu-toggle]');
      if (trigger) trigger.focus();
    }

    toggleBtns.forEach(function (btn) {
      btn.addEventListener('click', function () {
        var isOpen = menu.getAttribute('aria-hidden') === 'false';
        if (isOpen) { close(); } else { open(); }
      });
    });

    menu.addEventListener('keydown', function (e) {
      if (e.key === 'Escape') close();
    });

    menu.addEventListener('click', function (e) {
      if (!e.target.closest('a, button')) close();
    });
  });
</script>
